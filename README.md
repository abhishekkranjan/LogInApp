# LogInApp
Full-stack MERN App with user authentication using React.js, Node.js and MongoDB
Command to use this App:-
1) download repo
2) run npm install inside base folder and install dependencies
3) run npm install iniside client to install react libraries
4) run npm run dev to run the project
5) Go to https://localhost:3000 and enjoy the App.
